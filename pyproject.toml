[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "leaffliction"
version = "0.1.0"
description = "Leaffliction — leaf image classification (42 computer vision module)"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.8.0",
    "numpy>=1.24.0",
    "pillow>=10.0.0",
    "typer>=0.12.5",
    "pyside6>=6.11.1",
    "altair>=5.5.0",
    "opencv-python",
    "pandas>=3.0.2",
    "plantcv",
]

[project.scripts]
dist = "Distribution:main"
aug = "Augmentation:main"
xfm = "Transformation:main"
tr = "train:main"
pr = "predict:main"

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "opencv-stubs>=0.1.3",
]

[tool.setuptools]
package-dir = { "" = "src" }
py-modules = [
    "Distribution",
    "Augmentation",
    "Transformation",
    "train",
    "predict",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["distribution*"]

[tool.uv]
link-mode = "copy"

[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = ["src"]
pythonVersion = "3.12"
typeCheckingMode = "basic"
